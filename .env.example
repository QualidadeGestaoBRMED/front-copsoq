# Base da API do gate de participação (cpf_gate), com o prefixo /api/v2.
# Sem essa variável o app usa um mock local (CPF 14323631740 / passaporte AB123456 elegíveis).
# Staging: https://copsoq-quest-api-staging.onrender.com/api/v2
# Atenção: o domínio do front precisa estar no CORS_ALLOWED_ORIGINS do backend.
NEXT_PUBLIC_API_BASE_URL=
